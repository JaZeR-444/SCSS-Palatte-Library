{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Copy this file to settings.local.json. settings.local.json is gitignored and machine-specific. Never commit it.",
  "permissions": {
    "allow": [
      "Bash(npm run dev)"
    ]
  }
}
