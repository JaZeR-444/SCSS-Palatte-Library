# AI Scratchpad

This is a temporary workspace for the AI agent. 

It is automatically ignored by version control (and often by the agent's context indexing via `.claudeignore`).

The agent can use this folder to:
- Output large debugging logs.
- Write intermediate data processing files.
- Test isolated code snippets before integrating them into the main project.
- Write draft versions of large documents.
